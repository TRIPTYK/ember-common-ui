<TpkCheckbox
  @checked={{changeset-get @changeset @validationField}}
  @label={{@label}}
  @type={{@type}}
  @classless={{@classless}}
  @value={{@value}}
  @onChange={{this.onChange}}
  data-has-error='{{this.hasError}}'
  as |I|
>
  {{#if (has-block-params)}}
    {{yield
      (hash
        Input=I.Input
        Label=I.Label
        errors=this.errors
        hasError=this.hasError
        firstError=this.firstError
      )
    }}
  {{else}}
    <div class={{if @classless 'tpk-validation-checkbox-error-container'}}>
      <I.Input aria-invalid={{this.hasError}} />
      <I.Label>
        {{@label}}
      </I.Label>
      {{#if this.hasError}}
        {{#if (has-block 'errors')}}
          {{yield this.errors to='errors'}}
        {{else}}
          <span class={{unless @classless 'tpk-validation-checkbox-error'}}>
            {{this.firstError}}
          </span>
        {{/if}}
      {{/if}}
    </div>
    {{yield}}
  {{/if}}
</TpkCheckbox>