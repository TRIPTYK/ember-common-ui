<TpkInput
  @value={{changeset-get @changeset @validationField}}
  @label={{@label}}
  @type={{@type}}
  @classless={{@classless}}
  data-has-error='{{this.hasError}}'
  as |I|
>
  {{#if (has-block-params)}}
    {{yield
      (hash
        Input=I.Input
        Label=I.Label
        errors=this.errors
        hasError=this.hasError
        firstError=this.firstError
      )
    }}
  {{else}}
    <I.Label>
      {{@label}}
    </I.Label>
    <div class={{unless @classless 'tpk-validation-input-error-container'}}>
      <I.Input aria-invalid={{this.hasError}} @onChange={{this.onChange}} />
      {{#if this.hasError}}
        {{#if (has-block 'errors')}}
          {{yield this.errors to='errors'}}
        {{else}}
          <span class={{unless @classless 'tpk-validation-input-error'}}>
            {{this.firstError}}
          </span>
        {{/if}}
      {{/if}}
    </div>
    {{yield}}
  {{/if}}
</TpkInput>