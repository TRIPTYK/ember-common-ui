<div class={{unless @classless 'tpk-input'}} ...attributes data-test-tpk-input>
  {{#if (has-block)}}
    {{yield
      (hash
        Input=(component
          'tpk-input/input'
          onChange=this.onChange
          type=@type
          value=@value
          guid=this.guid
          classless=@classless
        )
        Label=(component
          'tpk-input/label'
          onChange=this.onChange
          guid=this.guid
          classless=@classless
        )
        guid=this.guid
      )
    }}
  {{else}}
    {{! Quick version }}
    <TpkInput::Label @guid={{@guid}} @classless={{@classless}}>
      {{@label}}
    </TpkInput::Label>
    <TpkInput::Input
      @value={{@value}}
      @type={{@type}}
      @onChange={{this.onChange}}
      @guid={{@guid}}
      @classless={{@classless}}
    />
  {{/if}}
</div>