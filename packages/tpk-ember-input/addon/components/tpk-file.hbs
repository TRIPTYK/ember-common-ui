<div class={{unless @classless 'tpk-file'}} ...attributes data-test-tpk-file>
  {{#if (has-block)}}
    {{yield
      (hash
        Input=(component
          'tpk-file/input'
          onChange=this.onChange
          accept=@accept
          guid=this.guid
          classless=@classless
        )
        Label=(component
          'tpk-file/label'
          onChange=this.onChange
          guid=this.guid
          classless=@classless
        )
        guid=this.guid
        files=this.files
      )
    }}
  {{else}}
    {{! Quick version }}
    <TpkFile::Label @guid={{this.guid}} @classless={{@classless}}>
      {{@label}}
    </TpkFile::Label>
    <TpkFile::Input
      accept={{@accept}}
      @type={{@type}}
      @onChange={{this.onChange}}
      @guid={{this.guid}}
      @classless={{@classless}}
    />
  {{/if}}
</div>