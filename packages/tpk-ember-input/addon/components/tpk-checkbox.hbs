<div
  class={{unless @classless 'tpk-checkbox'}}
  ...attributes
  data-test-tpk-checkbox
>
  {{#if (has-block-params)}}
    {{yield
      (hash
        Label=(component
          'tpk-checkbox/label'
          guid=this.guid
          checked=@checked
          label=@label
          classless=@classless
        )
        Input=(component
          'tpk-checkbox/input'
          guid=this.guid
          checked=@checked
          onChange=this.onChange
          classless=@classless
        )
        guid=this.guid
      )
    }}
  {{else}}
    <TpkCheckbox::Input
      @guid={{this.guid}}
      @onChange={{this.onChange}}
      @checked={{@checked}}
      @classless={{@classless}}
    />
    <TpkCheckbox::Label
      @classless={{@classless}}
      @guid={{this.guid}}
      @label={{@label}}
    />
  {{/if}}
</div>