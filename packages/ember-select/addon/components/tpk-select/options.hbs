{{! template-lint-disable no-down-event-binding }}

<div
  tabindex='-1'
  role='listbox'
  id={{this.guid}}
  aria-multiselectable={{@multiple}}
  aria-activedescendant={{@activeChild.id}}
  aria-labelledby={{@labelId}}
  class={{unless @classless 'tpk-select-options'}}
  {{did-insert @registerOptionsDiv}}
  ...attributes
>
  {{#each @options as |o i|}}
    {{yield
      (component
        'tpk-select/options/option'
        option=o
        guid=@guid
        registerChild=@registerChild
        index=i
        activeChild=@activeChild
        selected=@selected
        multiple=@multiple
        onChange=@onChange
      )
    }}
  {{/each}}
</div>