<div class='tpk-textarea' ...attributes>
  {{#if (has-block)}}
    {{yield
      (hash
        Label=(component
          'ui/textarea/label' guid=this.guid mandatory=@mandatory label=@label
        )
        Area=(component
          'ui/textarea/area'
          guid=this.guid
          value=@value
          limit=@limit
          updateValue=this.updateValue
        )
        Counter=(component 'ui/textarea/counter' count=this.count limit=@limit)
        Tooltip=(component 'ui/textarea/tooltip' tooltip=@tooltip)
        guid=this.guid
        count=this.count
        updateValue=this.updateValue
      )
    }}
  {{else}}
    <Ui::Textarea::Label
      @mandatory={{@mandatory}}
      @guid={{this.guid}}
      @label={{@label}}
    />
    {{! template-lint-disable no-duplicate-id }}
    <Ui::Textarea::Area
      @guid={{this.guid}}
      @updateValue={{this.updateValue}}
      @limit={{@limit}}
      @value={{@value}}
    />
    {{#if @limit}}
      <Ui::Textarea::Counter @count={{this.count}} @limit={{@limit}} />
    {{/if}}
    <Ui::Textarea::Tooltip @tooltip={{@tooltip}} />
  {{/if}}
</div>