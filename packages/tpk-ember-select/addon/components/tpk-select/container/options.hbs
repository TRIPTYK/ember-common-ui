<ul
  tabindex='-1'
  role='listbox'
  aria-labelledby={{@guid}}
  aria-activedescendant='{{@guid}}-listbox-option-{{@selectedIndex}}'
  class={{unless @classless 'tpk-select-container-options'}}
  data-is-open='{{@isOpen}}'
  ...attributes
>
  {{#each @options as |o i|}}
    {{yield
      (component
        'tpk-select/container/options/option'
        option=o
        guid=@guid
        index=i
        selected=@selected
        multiple=@multiple
        selectElement=@selectElement
      )
    }}
  {{/each}}
</ul>